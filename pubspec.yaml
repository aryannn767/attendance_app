name: attendance_app
description: A Flutter attendance app with RFID and Bluetooth beacon functionality.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  get: ^4.6.5
  flutter_blue_plus: ^1.35.2
  cupertino_icons: ^1.0.2
  flutter_reactive_ble: ^5.4.0
  flutter_ble_peripheral: ^1.2.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
